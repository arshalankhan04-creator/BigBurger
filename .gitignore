# Dependencies
node_modules/
.pnp
.pnp.js

# Build output
dist/
dist-ssr/
build/

# Vite cache
.vite/

# Environment variables — NEVER commit these
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.production
.env.staging

# Secrets and credentials — NEVER commit these
*.pem
*.key
*.cert
*.p12
*.pfx
secrets/
credentials/

# Editor
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# OS
.DS_Store
.DS_Store?
Thumbs.db
ehthumbs.db

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# Reference images (design assets, not used in app)
src/assets/images/Loginpageexample*.png

# Local notes / private docs (keep outside project folder or add here)
For me/

# Supabase — local functions (may contain secrets / env vars)
supabase/functions/

# Supabase — test seed data (contains fake emails inserted into auth.users)
# Safe to keep locally for development; not needed publicly
supabase/seed_test_data.sql
